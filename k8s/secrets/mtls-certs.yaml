---
# CA证书Secret（所有服务共享）
apiVersion: v1
kind: Secret
metadata:
  name: qilin-ca-cert
  namespace: default
  labels:
    app: qilin-stack
    component: security
type: Opaque
data:
  ca-cert.pem: |
    # Base64编码的CA证书，由cert_manager.py生成后替换
    # cat /etc/qilin/certs/ca-cert.pem | base64 -w 0

---
# API Gateway证书
apiVersion: v1
kind: Secret
metadata:
  name: api-gateway-tls
  namespace: default
  labels:
    app: qilin-stack
    service: api-gateway
type: kubernetes.io/tls
data:
  tls.crt: |
    # Base64编码的证书
  tls.key: |
    # Base64编码的私钥

---
# Feature Engine证书
apiVersion: v1
kind: Secret
metadata:
  name: feature-engine-tls
  namespace: default
  labels:
    app: qilin-stack
    service: feature-engine
type: kubernetes.io/tls
data:
  tls.crt: |
    # Base64编码的证书
  tls.key: |
    # Base64编码的私钥

---
# Agent Orchestrator证书
apiVersion: v1
kind: Secret
metadata:
  name: agent-orchestrator-tls
  namespace: default
  labels:
    app: qilin-stack
    service: agent-orchestrator
type: kubernetes.io/tls
data:
  tls.crt: |
    # Base64编码的证书
  tls.key: |
    # Base64编码的私钥

---
# Risk Controller证书
apiVersion: v1
kind: Secret
metadata:
  name: risk-controller-tls
  namespace: default
  labels:
    app: qilin-stack
    service: risk-controller
type: kubernetes.io/tls
data:
  tls.crt: |
    # Base64编码的证书
  tls.key: |
    # Base64编码的私钥
